# 贪吃蛇游戏 · 实现报告

> 本文档对 `tanchishe.html` 的**最终实现**做逐模块解读，说明每个模块「怎么实现的」「在整体架构中的位置」以及「为什么这样做」。
> 配套文档：`贪吃蛇游戏模块设计.md`（模块划分与设计目标），本报告侧重**代码落地与设计取舍**。行号以当前提交版本为准。

---

## 一、总体架构

### 1.1 交付形态：单文件、零依赖

整个游戏封装在**一个 HTML 文件**里（HTML + CSS + JS 内嵌），无任何外部依赖、无构建工具，双击即可运行。

**为什么选单文件：** 游戏规模小（逻辑量约 700 行 JS），拆分多文件带来的收益（职责隔离、可扩展）低于成本（部署/分享/维护复杂度）。单文件便于分发与真机调试，符合「浏览器小游戏」的定位。设计文档第五节也明确将「方案 A：单文件」列为推荐。

### 1.2 核心架构原则：状态机驱动 + 数据/渲染分离

代码围绕两条主线组织：

1. **单一状态变量** `state`（`ready → running ⇄ paused`，`running → over / win`）集中管理生命周期，所有行为都由当前状态决定；
2. **数据与渲染分离**：每步先更新数据（蛇、食物、得分），再调用 `draw()` 一次性重绘。逻辑层不感知像素，渲染层不修改状态。

**为什么这样做：** 状态机把「何时能动、何时暂停、何时结束」收敛到少数几个函数（`start`/`pause`/`resume`/`gameOver`/`win`），避免散落的布尔标志；数据/渲染分离让 AI 演示、触控、键盘等输入源都能复用同一套 `step()`，无需各自重写移动与绘制逻辑。

### 1.3 页面布局：三栏 + 响应式

`.layout`（flex row + `flex-wrap`）包裹三块：

- 左侧面板：历史最高分；
- 中间 `.game`：标题、分数、成绩有效性提示、画布、控制按钮；
- 右侧面板：胜利目标设置。

`@media (max-width:900px)` 下 `.game` 通过 `order:-1` 置顶，两侧面板下移并 `width:100%`，实现窄屏（手机）从「三栏横排」平滑降级为「游戏优先的上下分区」。

---

## 二、逐模块实现与设计理由

### M1 · 网格与画布

```js
const CELL = 20;                 // 每格像素
const COLS = canvas.width / CELL; // 20
const ROWS = canvas.height / CELL; // 20
```

固定 20×20 网格、每格 20px，画布逻辑尺寸 400×400。坐标全部以「格」为单位（`{x,y}`），渲染时再乘以 `CELL` 换算像素。

**为什么用 Canvas 而非 DOM 网格：** 绘制性能更好，且逻辑（格坐标）与视觉（像素）解耦——移动、碰撞、AI 寻路全部只操作整数格坐标，`draw()` 独立负责视觉，互不干扰。这是设计文档 M1 的推荐方案。

### M2 · 主题系统（深色 / 浅色）

**双轨机制**，是这套实现里一个容易忽略但关键的细节：

- **CSS 变量**（`:root` 与 `[data-theme="light"]`）驱动 UI 组件（面板、按钮、文字、遮罩），通过切换 `<html data-theme>` 一键换肤；
- **JS 字面量 `COLORS`**（`gridAlt/snake/snakeTail/head/food/foodLight/foodGlow`）驱动 Canvas 绘制。原因是 **Canvas 的 `fillStyle/strokeStyle` 不支持 `var()`**，必须用具体颜色值。

`applyTheme(theme)` 用 `Object.assign(COLORS, t)` 一次性写入该主题全部色值，再 `draw()` 重绘，并写入 localStorage。颜色在 `THEMES` 对象里集中登记，CSS 变量与 JS 字面量两处色值一一对应，改动时需同步维护（设计文档 9.3 对此有标注）。

**为什么这么做：** 一条路（纯 CSS 变量）无法覆盖 Canvas，另一条（纯 JS）又会让 DOM 组件失去 CSS 的级联便利，于是拆成「UI 用 CSS 变量、Canvas 用 JS 字面量」的双轨，各自走最合适的机制，再由 `applyTheme` 统一调度。

### M3 · 蛇的数据结构与移动

```js
let snake = [
  { x: 9, y: 10 }, // [0] 为蛇头
  { x: 8, y: 10 },
  { x: 7, y: 10 },
];
```

**坐标数组**表示蛇身，`[0]` 是蛇头。移动算法（`step()` 内）：

1. 根据 `direction` 计算新蛇头 `newHead`；
2. **若未吃食物先 `snake.pop()` 移除尾部**，再判自撞；
3. `snake.unshift(newHead)` 头插；
4. 若吃到食物则**保留尾部**（即上一步不 pop），实现「增长」。

**为什么用数组头插尾删：** 相比用对象/链表，数组操作直观，且增长只需「暂缓删尾」一个分支，语义清晰。O(n) 的 `unshift` 在 400 格上限的规模下完全无性能压力。

### M4 · 方向控制与预输入队列（全项目最复杂的一处）

方向由**当前方向 `direction` + 预输入队列 `directionQueue`** 共同决定：

```js
const directionQueue = [];   // 元素 {x, y, at}
const MAX_QUEUE = 3;         // 队列最大长度
const QUEUE_TIMEOUT = 500;   // 预输入超时(ms)
```

三个机制环环相扣：

1. **入队 `queueDirection(next)`**：反向保护基准取「队尾（队列非空）或当前方向（队列空）」——即新方向不能与其**实际执行顺序中的前一个方向**相反；队列已满时**丢弃本次新输入**（而非丢弃队头）。
2. **消费（`step()` 内）**：先弹出超时队头（`now - at > 500ms`），再取队头作为新方向；取用前再兜底跳过与当前方向相反的队头。
3. **关键不变量**：执行序列（当前方向 → 队头 → … → 队尾）中任意相邻两方向均不反向。

**为什么选 3 个、为什么「满则丢弃新输入」：** 这里踩过实际 bug（见 README）。最初队列长度取 2 且「满时 `shift()` 丢弃队头」，结果快速连按四个方向（上→左→下→右）后队头被顶掉，残留方向与当前方向相反，蛇在下一步原地掉头撞入身体。根因是「丢队头」破坏了「队头与当前方向不反向」的不变量。因此最终方案改为：**长度扩到 3 + 满则丢新输入 + 消费时兜底跳过反向队头**，从两个方向同时守住不变量。这是「设计文档与代码同步」约定的典型体现——修复与文档（M7 预输入队列小节）同步补记。

### M5 · 食物系统

`spawnFood()` 先构造蛇身占位集合 `occupied`，然后**随机取点 + do-while 重试**直到落在空白格；若蛇已占满网格（`occupied.size >= COLS*ROWS`）直接返回（此时应已触发胜利，属防御分支）。

**为什么用「随机重试」而非「列出所有空位再抽」：** 绝大多数情况下空白格占比很高，重试期望次数极低、代码更短；只在极端满盘时理论上有小概率多次重试，但此时受胜利判定兜底，不会死循环。

### M6 · 碰撞检测

三类判定集中在 `step()` 内、按严格顺序执行：

1. **撞墙**：`newHead` 越界 → `gameOver()`；
2. **撞自身**：`snake.some(...)` 匹配新蛇头（**在移除尾部之后**判定）；
3. **吃到食物**：`newHead === food` → 加分 + 增长 + 胜利判定 + 生成新食物。

**为什么「先移尾再判自撞」：** 蛇头移动到「即将被移除的尾部」那一格是合法移动。若先判自撞，会把这步误判为撞身。设计文档 M6 专门用「> 注意」标注了这一点，代码在 `!ate` 分支先 `pop()` 再 `some()`，严格执行。

### M7 · 游戏状态机与主循环

```js
let state = 'ready';   // ready | running | paused | over | win
let timer = null;
const SPEED = 150;     // 每步间隔(ms)
```

- **主循环用 `setInterval(step, SPEED)`**，暂停/结束即 `clearInterval`。`step()` 开头 `if (state !== 'running') return` 双重防护（即便计时器未及时清除也不会错误推进）。
- 状态转换集中在 `start` / `pause` / `resume` / `togglePause` / `gameOver` / `win` 六个函数；`renderState()` 根据状态刷新遮罩与按钮文案/禁用态。
- 状态机在原始「ready → running ⇄ paused → over」基础上**扩展了 `win` 状态**，与 `over` 同构（清计时器 + 绘制 + 刷新遮罩），所有「重新开始」入口（空格键、按钮）将 `win` 与 `over` 同等对待。

**为什么用 `setInterval` 而非 `requestAnimationFrame`：** 本游戏移动以「格」为单位、节奏固定，不需要逐帧平滑动画，`setInterval` 语义直接、代码量最小。设计文档 M8 明确给出「推荐 setInterval（简单）」与「rAF+时间累积（更平滑）」两个选项，本实现选了前者，符合规模。

### M8 · 计分与胜利目标

- 得分 `score++`，`updateScore()` 实时刷新 DOM；
- 胜利目标 `winScore`（按得分计）可配置：范围 `[1, 397]`、默认 397（`WIN_MIN=1`、`WIN_MAX=COLS*ROWS-3`、`WIN_DEFAULT=WIN_MAX`），`clampWin()` 统一夹取；
- 判定在 `step()` 吃食物加分后、`spawnFood()` 之前：`if (score >= winScore) return win()`。

**为什么把胜利目标做成可配置（且按得分而非蛇长）：** 默认「填满地图即胜」对普通玩家近乎不可达，暴露为右侧面板（数字直输框 + 四个独立步进按钮 `-5/-1/+1/+5` + 预设 20/50/100/200/397）后，玩家可按自己的目标挑战；以得分作为目标比蛇身长度更直观（得分就是面板主显示的数字，蛇长需扣除初始 3 节）。持久化用 localStorage `snake-win`，读写套 try/catch（见 M10）。设计文档第九节对此有专章。

### M9 · 历史得分记录

- 数据模型：localStorage `snake-history`，值为 `{score, ts, result}` 数组，`result ∈ 'over'|'win'`；上限 `HISTORY_MAX=100`；
- `recordScore(score, result)` 在 `gameOver()`/`win()` 两个终态末尾调用（一局只进一次终态，天然不重复）；
- **过滤规则**：`gameUsedAi` 为真不计入；`score <= 0` 不记录；
- 追加后 `sort`（分数降序，同分 `ts` 新在前）→ 截断到 100 → 持久化 → 刷新；
- 界面：左栏常驻前 10 高分，modal 展示完整列表；DOM 用 `createElement` 构建（而非 innerHTML 字符串拼接），规避 XSS。

**关键设计 `gameUsedAi`（「沾过即作废」）**：`start()` 置 `false`，`step()` 中一旦 `aiMode` 为真即**永久置 `true`**。之所以不采用「当前是否 AI」或「运行中禁用开关」，是因为允许玩家中途自由开关 AI，必须堵住「AI 玩到高分后中途关 AI 洗白成绩」的漏洞——一旦沾过 AI，整局成绩作废。这是用户在三个候选中选定的方案（详见设计文档第十一节与记忆 [[history-score-record]]）。

### M10 · 数据持久化（三处，统一容错）

三处 localStorage：`snake-theme`、`snake-win`、`snake-history`。**每一处读写都套 try/catch，失败静默降级**（如隐私模式 / `file://` 下存储不可用时，游戏仍可用，仅不持久化）。

**为什么逐处 try/catch：** 单文件 + 双击打开的部署方式意味着很可能运行在 `file://` 或禁 cookie 环境，一次未捕获的 `localStorage` 异常就会让整个脚本中断、游戏无法启动。把存储访问收敛到带容错的 helper（`loadTheme`/`saveTheme`/`loadWinLength`/`saveWinLength`/`loadHistory`/`saveHistory`），把「不可用」当作正常路径处理。

### M11 · AI 演示（确定性搜索 bot）

核心是 `aiNextDir()` 的**三级优先级决策**（设计文档第十节）：

1. **优先——安全吃食**：`bfs(head, food, obstacles)` 求最短路径，再 `safeToFollow(foodPath)` 模拟走完该路径后蛇头是否仍连通蛇尾（防止自围）；满足则沿路径走第一步。
2. **次选——跟随蛇尾**：`bfs(head, tail, obstacles)` 绕圈等待机会。
3. **兜底——最大空间**：`bestOpenSpaceMove()` 用洪水填充评估每个可行方向的剩余可达空间，取最大者。

辅助函数：`bfs`（最短路径）、`floodFill`（可达空格计数）、`safeToFollow`（吃食后连通性模拟）、`dirFromTo`、`bestOpenSpaceMove`。

**接入方式（核心逻辑零改动）**：`step()` 内 `if (aiMode)` 分支直接调用 `aiNextDir()` 覆盖 `direction` 并清空输入队列；键盘/触控在 `aiMode` 下被 `!aiMode` 守卫忽略。完全复用现有 `step`/`draw`/`renderState` 与状态机，移动、碰撞、计分逻辑不动。

**关键细节——「尾部视为空位」**：`obstacles` 由 `snake.slice(0, -1)` 构造，把尾部排除在障碍外。因为蛇每步移动时尾部会被移除（除非吃食），寻路时可把尾部当作可走格子，避免 BFS 因「尾部暂时占格」而找不到本可实现的路径。

**为什么选搜索 bot 而非 DQN 强化学习：** 用户在两条路线中选定方案 A。搜索 bot 训练成本为零、完全确定性、实测 50 局 100% ≥15 分（最低 31、中位 150），契合「可靠演示」目标；DQN 需离线训练 + 调参 + JS 导出，且 RL 蛇普遍在 20–40 分出现平台期，对演示价值反而更低（详见记忆 [[ai-demo-decision]]）。

### M12 · 输入处理（键盘 + 触控并存）

- **键盘**：`keydown` 监听，方向键/WASD 映射到 `KEY_DIRS`，仅 `state==='running' && !aiMode` 时入队；空格开始/重开，Esc 优先关 modal、否则切暂停。
- **触控**：`touchstart` 记起点、`touchend` 算位移；位移 <20px 视为点击忽略；取位移更大的轴向映射为 `{x,y}` 方向向量，同样走 `queueDirection()`。

**为什么触控复用 `queueDirection`：** 键盘与滑动最终都归一为 `{x,y}` 方向向量，反向保护、长度限制、超时丢弃一次生效，无需为触控重写一套逻辑，天然与「M5 输入共存」原则一致。配合 `touch-action:none`（画布防滚动缩放）、`touch-action:manipulation`（按钮防双击缩放）、`overscroll-behavior:none`、`user-select:none` 等触控人体工学设置（设计文档第八节）。

### M13 · 渲染与遮罩

- `draw(now)`：`clearRect` 清屏 → 棋盘格底纹 → 食物（`drawFood`：径向渐变 + 高光 + 呼吸脉动）→ 蛇身（`drawSnake`：中心粗线圆头 + 头亮尾暗渐变）→ 蛇头（`drawHead`：圆角块 + 朝向眼睛）。
- **持续渲染**：`requestAnimationFrame(renderLoop)` 每帧重绘，驱动食物呼吸动画；状态更新仍由 `step` 的 `setInterval` 负责，两者职责分离。
- `renderState()`：根据 `state` 刷新 `.overlay` 遮罩的标题（`danger`/`success` 配色）、副文案、按钮显隐与禁用态，五种状态各有分支。
- 成绩有效性提示 `renderValidity()`：依据 `gameUsedAi` 渲染「本局成绩有效/无效」，绿/红配色，在 `start()` 与 `step()` 沾 AI 两处刷新。

**为什么全量重绘 + rAF 持续渲染：** 网格规模小、绘制廉价，全量重绘逻辑最简单且不易出错；引入 rAF 持续渲染是「数据/渲染分离」的强化——`step` 只更新数据，渲染循环只负责画，互不侵入。

---

## 三、设计取舍总览

| 议题 | 采用的方案 | 被放弃/搁置的方案 | 理由 |
|------|-----------|------------------|------|
| 交付形态 | 单文件内嵌 | 多文件拆分 | 规模小，分享/调试优先 |
| 渲染 | Canvas | DOM 网格 | 性能 + 逻辑视觉解耦 |
| 蛇结构 | 坐标数组头插尾删 | 链表 / 对象 | 直观、规模小无性能压力 |
| 主循环 | `setInterval` 固定步长 | `rAF`+时间累积 | 格状移动无需平滑帧 |
| 预输入队列 | 长度 3 + 满则丢新 + 兜底跳过 | 长度 2 + 满则丢队头 | 后者破坏不变量，实测出 bug |
| 食物生成 | 随机重试 | 枚举空位抽签 | 空位占比高，重试期望极低 |
| 主题 | CSS 变量 + JS 字面量双轨 | 单一机制 | Canvas 不支持 `var()` |
| AI 演示 | 确定性 BFS 搜索 bot | DQN 强化学习 | 零成本、确定性、可靠达标 |
| AI 成绩判定 | `gameUsedAi` 沾过即作废 | 当前是否 AI / 禁用开关 | 堵住「中途关 AI 洗白」漏洞 |
| 持久化 | 逐处 try/catch 静默降级 | 裸调用 | 兼容 file:// 与隐私模式 |

---

## 四、遗留与可改进点

1. **高 DPI 模糊**：当前用 CSS 缩放画布，高 DPI 屏下略糊；可升级为 `devicePixelRatio` 方案（设计文档 8.4 已记录）。
2. **虚拟方向键未实现**：移动端只有滑动输入，不习惯滑动者暂无 D-pad。
3. **音效未实现**：设计文档 M10 列为可选的 Web Audio 音效尚未落地。
4. **速度递增未实现**：设计文档 M9 提到「随分数加速」，当前 `SPEED` 固定 150ms。
5. **极端预输入边界**：README 提到队列方案「仅在极端情况下仍可能触发」掉头 bug，属已知极小概率残留。

---

## 五、结论

`tanchishe.html` 的实现始终围绕一条主线：**用最少的机制满足需求，并为每个机制守住一条不变量**。状态机收束生命周期，数据/渲染分离让键盘、触控、AI 三种输入源共用同一套移动逻辑；预输入队列的不变量（相邻方向不反向）和成绩判定的不变量（沾过 AI 即作废）都是「先立规矩、再在入队/消费两端共同守护」的典型。确定性搜索 bot 与逐处容错的持久化，则分别体现了「演示优先于炫技」「健壮优先于简洁」的取舍。整体上，代码与设计文档（`贪吃蛇游戏模块设计.md`）各章节一一对应，保持同步，便于后续扩展（如 DQN 升级、速度递增、音效）。
