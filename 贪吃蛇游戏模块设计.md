# 贪吃蛇小游戏 · 模块设计文档

## 一、项目概述

制作一个运行在浏览器中的经典贪吃蛇小游戏。玩家通过方向键控制蛇的移动，吃到食物后蛇身增长并得分，撞到墙壁或自身则游戏结束。

- **目标平台**：浏览器（桌面端为主，兼顾移动端）
- **技术栈**：纯 HTML + CSS + JavaScript，零依赖，无需构建工具
- **交付形态**：单文件 `index.html` 可直接双击打开运行（推荐），也可拆分为多文件

---

## 二、模块划分总览

| 模块 | 职责 | 优先级 |
|------|------|--------|
| M1 页面结构（HTML） | 布局骨架、UI 元素 | 必需 |
| M2 样式（CSS） | 视觉呈现、响应式 | 必需 |
| M3 游戏状态机 | 管理游戏生命周期 | 必需 |
| M4 蛇对象与移动 | 蛇的数据结构、方向控制 | 必需 |
| M5 食物系统 | 食物生成与碰撞 | 必需 |
| M6 碰撞检测 | 边界 / 自身 / 食物判定 | 必需 |
| M7 输入处理 | 键盘与移动端触控 | 必需 |
| M8 游戏主循环 | 渲染与更新节奏、速度控制 | 必需 |
| M9 计分与难度 | 得分、最高分、速度递增 | 必需 |
| M10 音效 | 吃食物 / 死亡音效 | 可选 |
| M11 数据持久化 | localStorage 记录最高分 | 可选 |

---

## 三、模块详细说明

### M1 · 页面结构（HTML）

- 游戏主容器（居中）
- 渲染画布：`<canvas>` 或 CSS Grid 网格。**推荐 `<canvas>`**，绘制性能更好、逻辑与视觉解耦。
- 信息栏：当前得分、最高分
- 控制按钮：开始 / 暂停 / 重新开始
- 状态遮罩：游戏结束 / 暂停时的半透明提示层

### M2 · 样式（CSS）

- 整体布局：flex/grid 居中，深色主题背景
- 蛇身、食物、网格的配色与形状
- 按钮 hover/active 反馈
- 响应式：小屏（手机）下缩放画布，并提供触控按钮
- 过渡动画：得分变化、死亡闪屏等（可选）

### M3 · 游戏状态机（Game State）

用单一状态变量控制生命周期，避免逻辑混乱：

```
ready（就绪） → running（运行中） ⇄ paused（暂停）
running ─────→ gameover（结束） → ready
```

- 每个状态的进入/退出有明确行为（如开始计时、清除遮罩）
- 状态转换集中在少数函数中（`start()` / `pause()` / `reset()` / `gameOver()`）

### M4 · 蛇对象与移动

- 数据结构：用**坐标数组**表示蛇身，如 `[{x,y}, {x,y}, ...]`，`[0]` 为蛇头。
- 移动算法：
  1. 在蛇头前方（当前方向）计算新蛇头坐标
  2. 将新蛇头插入数组头部
  3. 若未吃到食物则移除尾部；否则保留尾部（实现增长）
- 方向控制：维护 `currentDirection`，禁止 180° 反向（如向左时不能直接向右）

### M5 · 食物系统

- 在网格内的**空白位置**随机生成食物
- 使用随机坐标，若与蛇身重叠则重新生成（避免食物出现在蛇身上）
- 食物可携带分值（普通食物 +1，可选特殊食物更高分、限时）

### M6 · 碰撞检测

三类判定，每帧移动后检查：

1. **撞墙**：蛇头坐标越出网格边界
2. **撞自身**：新蛇头与蛇身（除尾部分）任一节重叠
3. **吃到食物**：新蛇头与食物坐标重叠 → 触发增长 + 加分 + 生成新食物

> 注意：若蛇头移动后恰与「即将移除的尾部」重叠，属合法移动，应先移除尾部再判定自撞，避免误判。

### M7 · 输入处理

- 键盘：方向键（↑↓←→）与 WASD
- 反向保护：忽略与当前方向相反的输入
- 输入缓冲：快速连按可入队，避免快速转向丢失
- 移动端：滑动手势或屏幕虚拟方向键（可选）
- 暂停/开始等快捷键（空格键）

### M8 · 游戏主循环

- 推荐 `setInterval` 搭配可变间隔（简单）或 `requestAnimationFrame` + 时间累积（更平滑、帧率稳定）
- 速度控制：移动间隔随分数递减（如每得 5 分加速一次），设上下限
- 循环体：`读取输入 → 更新蛇位置 → 碰撞检测 → 重绘画面`

### M9 · 计分与难度

- 吃食物加分，实时更新界面
- 最高分（Best Score）：从 localStorage 读取/写入
- 难度递增：得分越高移动越快

### M10 · 音效（可选）

- 使用 Web Audio API 生成简单音效（无需音频文件）：吃食物「滴」、死亡「低沉音」
- 提供静音开关

### M11 · 数据持久化（可选）

- `localStorage` 保存最高分，刷新/重开页面后仍保留

---

## 四、模块间关系与数据流

```
键盘/触控输入 (M7)
      │  方向指令
      ▼
游戏状态机 (M3) ◄──── 按钮/生命周期事件
      │  允许移动?
      ▼
主循环 (M8) ──更新──► 蛇移动 (M4) ──► 碰撞检测 (M6)
      │                    │               │
      │                    │            吃到食物?
      │                    ▼               │
      │              食物系统 (M5) ◄────────┘  撞墙/自身?
      │               (生成新食物)              │
      ▼                                        ▼
   重绘画布 (M1/M2)                     计分 (M9) / 结束 (M3)
```

核心是「**更新逻辑与渲染分离**」：每帧先更新数据（蛇、食物、得分），再根据数据一次性绘制画面，保证逻辑清晰、易于调试。

---

## 五、文件结构建议

**方案 A：单文件（推荐）**
```
index.html          # HTML + CSS + JS 全部内嵌
```
优点：双击即玩、便于分享，适合该规模游戏。

**方案 B：分离文件**
```
index.html          # 页面结构
css/style.css       # 样式
js/game.js          # 游戏逻辑（状态机、蛇、食物、循环等）
js/input.js         # 输入处理（可选再拆）
```
优点：职责清晰、便于扩展。

---

## 六、推荐实现步骤

1. 搭建页面骨架与画布，绘制静态网格
2. 实现蛇的数据结构与渲染
3. 实现主循环 + 键盘输入，让蛇动起来
4. 加入食物生成与吃食物增长
5. 加入碰撞检测（撞墙 / 自撞）与游戏结束
6. 加入计分、最高分、难度递增
7. 完善状态机（开始/暂停/重启）与 UI
8. （可选）音效、移动端适配、视觉打磨

---

## 七、关键设计要点小结

- 采用**网格坐标系**，移动按格子为单位，逻辑简单清晰
- 蛇身用**数组 + 头插尾删**实现，增长即「暂缓删尾」
- **先移尾再判自撞**，避免合法移动被误判
- 状态机集中管理生命周期，输入、循环、碰撞各司其职
- 更新与渲染分离，便于维护和调试

---

## 八、移动端适配

### 8.1 所需模块

| 模块 | 职责 | 关键点 |
|------|------|--------|
| M1 响应式画布 | 固定尺寸 → 自适应视口 | `width:400px; max-width:100%; height:auto` 保持正方形 |
| M2 触控输入 | 滑动转向 | 复用 `queueDirection()`，无需改核心逻辑 |
| M3 触控人体工学 | 防缩放 / 防滚动 / 防误触 | `touch-action`、去高亮、`overscroll-behavior`、≥44px 按钮 |
| M4 布局与文案 | 小屏重排、提示更新 | 按钮换行、竖屏上下分区 |
| M5 输入共存 | 键盘 + 触控并存 | 同一套方向数据结构，互不冲突 |

### 8.2 关键实现

- **滑动检测**：`touchstart` 记录起点，`touchend` 计算位移；位移低于 20px 视为点击忽略；取位移更大的轴向映射为 `{x,y}` 方向向量。
- **复用入队**：触控产生 `{x,y}` 后同样走 `queueDirection()`，反向保护 / 长度限制 / 超时丢弃自动生效。
- **防误触**：画布 `touch-action:none` 阻止滚动缩放；按钮 `touch-action:manipulation` 防双击缩放；`overscroll-behavior:none` 防下拉回弹；`user-select:none` 防滑动选中。

### 8.3 实施步骤

1. 响应式画布（CSS 缩放）
2. 滑动输入（touchstart/touchend → `queueDirection`）
3. 触控优化（`touch-action`、去高亮、防滚动）
4. 布局与文案适配（小屏重排、提示更新）
5. 真机测试（iOS Safari / Android Chrome）

### 8.4 已知取舍

- 当前采用 **CSS 缩放（方案 A）**：实现简单，高 DPI 屏画布略有模糊；如需锐利可升级为 JS `devicePixelRatio` 方案。
- 虚拟方向键（D-pad）暂未实现，滑动不习惯时可后续补充。
