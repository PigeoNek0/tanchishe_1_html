# 贪吃蛇小游戏 · 模块设计文档

## 一、项目概述

制作一个运行在浏览器中的经典贪吃蛇小游戏。玩家通过方向键控制蛇的移动，吃到食物后蛇身增长并得分，撞到墙壁或自身则游戏结束。

- **目标平台**：浏览器（桌面端为主，兼顾移动端）
- **技术栈**：纯 HTML + CSS + JavaScript，零依赖，无需构建工具
- **交付形态**：单文件 `index.html` 可直接双击打开运行（推荐），也可拆分为多文件

---

## 二、模块划分总览

| 模块 | 职责 | 优先级 |
|------|------|--------|
| M1 页面结构（HTML） | 布局骨架、UI 元素 | 必需 |
| M2 样式（CSS） | 视觉呈现、响应式 | 必需 |
| M3 游戏状态机 | 管理游戏生命周期 | 必需 |
| M4 蛇对象与移动 | 蛇的数据结构、方向控制 | 必需 |
| M5 食物系统 | 食物生成与碰撞 | 必需 |
| M6 碰撞检测 | 边界 / 自身 / 食物判定 | 必需 |
| M7 输入处理 | 键盘与移动端触控 | 必需 |
| M8 游戏主循环 | 渲染与更新节奏、速度控制 | 必需 |
| M9 计分与难度 | 得分、最高分、速度递增 | 必需 |
| M10 音效 | 吃食物 / 死亡音效 | 可选 |
| M11 数据持久化 | localStorage 记录最高分 | 可选 |

---

## 三、模块详细说明

### M1 · 页面结构（HTML）

- 游戏主容器（居中）
- 渲染画布：`<canvas>` 或 CSS Grid 网格。**推荐 `<canvas>`**，绘制性能更好、逻辑与视觉解耦。
- 信息栏：当前得分、最高分
- 控制按钮：开始 / 暂停 / 重新开始
- 状态遮罩：游戏结束 / 暂停时的半透明提示层

### M2 · 样式（CSS）

- 整体布局：flex/grid 居中，深色主题背景
- 蛇身、食物、网格的配色与形状
- 按钮 hover/active 反馈
- 响应式：小屏（手机）下缩放画布，并提供触控按钮
- 过渡动画：得分变化、死亡闪屏等（可选）

### M3 · 游戏状态机（Game State）

用单一状态变量控制生命周期，避免逻辑混乱：

```
ready（就绪） → running（运行中） ⇄ paused（暂停）
running ─────→ gameover（结束） → ready
```

- 每个状态的进入/退出有明确行为（如开始计时、清除遮罩）
- 状态转换集中在少数函数中（`start()` / `pause()` / `reset()` / `gameOver()`）

### M4 · 蛇对象与移动

- 数据结构：用**坐标数组**表示蛇身，如 `[{x,y}, {x,y}, ...]`，`[0]` 为蛇头。
- 移动算法：
  1. 在蛇头前方（当前方向）计算新蛇头坐标
  2. 将新蛇头插入数组头部
  3. 若未吃到食物则移除尾部；否则保留尾部（实现增长）
- 方向控制：维护 `currentDirection`，禁止 180° 反向（如向左时不能直接向右）

### M5 · 食物系统

- 在网格内的**空白位置**随机生成食物
- 使用随机坐标，若与蛇身重叠则重新生成（避免食物出现在蛇身上）
- 食物可携带分值（普通食物 +1，可选特殊食物更高分、限时）

### M6 · 碰撞检测

三类判定，每帧移动后检查：

1. **撞墙**：蛇头坐标越出网格边界
2. **撞自身**：新蛇头与蛇身（除尾部分）任一节重叠
3. **吃到食物**：新蛇头与食物坐标重叠 → 触发增长 + 加分 + 生成新食物

> 注意：若蛇头移动后恰与「即将移除的尾部」重叠，属合法移动，应先移除尾部再判定自撞，避免误判。

### M7 · 输入处理

- 键盘：方向键（↑↓←→）与 WASD
- 反向保护：忽略与当前方向相反的输入
- 输入缓冲：快速连按可入队，避免快速转向丢失
- 移动端：滑动手势或屏幕虚拟方向键（可选）
- 暂停/开始等快捷键（空格键）

#### 预输入队列的实现要点

- 结构：`directionQueue` 存放 `{x, y, at}`，`MAX_QUEUE = 3`，`QUEUE_TIMEOUT = 500ms`。
- 入队 `queueDirection()`：
  - 反向保护基准取「队尾（队列非空）或当前方向（队列空）」——即新方向不能与其**实际执行顺序中的前一个方向**相反。
  - 队列已满时**丢弃本次输入**，而非丢弃队头。
- 消费（`step()` 内）：先丢弃超时队头，再取出队头作为新方向。
- **关键不变量**：执行序列（当前方向 → 队头 → … → 队尾）中任意相邻两方向均不反向。该不变量由「入队按序校验」与「消费时跳过反向队头」共同保证。
- **曾修复的 bug**：原实现队列满时 `shift()` 丢弃队头，会破坏「队头与当前方向不反向」的不变量——快速连按四个方向（如 上→左→下→右）后，队头被顶掉，残留的「左」与当前方向「右」相反，执行时原地掉头撞入身体。现改为满则丢弃新输入 + 消费时兜底跳过反向队头。

### M8 · 游戏主循环

- 推荐 `setInterval` 搭配可变间隔（简单）或 `requestAnimationFrame` + 时间累积（更平滑、帧率稳定）
- 速度控制：移动间隔随分数递减（如每得 5 分加速一次），设上下限
- 循环体：`读取输入 → 更新蛇位置 → 碰撞检测 → 重绘画面`

### M9 · 计分与难度

- 吃食物加分，实时更新界面
- 最高分（Best Score）：从 localStorage 读取/写入
- 难度递增：得分越高移动越快

### M10 · 音效（可选）

- 使用 Web Audio API 生成简单音效（无需音频文件）：吃食物「滴」、死亡「低沉音」
- 提供静音开关

### M11 · 数据持久化（可选）

- `localStorage` 保存最高分，刷新/重开页面后仍保留

---

## 四、模块间关系与数据流

```
键盘/触控输入 (M7)
      │  方向指令
      ▼
游戏状态机 (M3) ◄──── 按钮/生命周期事件
      │  允许移动?
      ▼
主循环 (M8) ──更新──► 蛇移动 (M4) ──► 碰撞检测 (M6)
      │                    │               │
      │                    │            吃到食物?
      │                    ▼               │
      │              食物系统 (M5) ◄────────┘  撞墙/自身?
      │               (生成新食物)              │
      ▼                                        ▼
   重绘画布 (M1/M2)                     计分 (M9) / 结束 (M3)
```

核心是「**更新逻辑与渲染分离**」：每帧先更新数据（蛇、食物、得分），再根据数据一次性绘制画面，保证逻辑清晰、易于调试。

---

## 五、文件结构建议

**方案 A：单文件（推荐）**
```
index.html          # HTML + CSS + JS 全部内嵌
```
优点：双击即玩、便于分享，适合该规模游戏。

**方案 B：分离文件**
```
index.html          # 页面结构
css/style.css       # 样式
js/game.js          # 游戏逻辑（状态机、蛇、食物、循环等）
js/input.js         # 输入处理（可选再拆）
```
优点：职责清晰、便于扩展。

---

## 六、推荐实现步骤

1. 搭建页面骨架与画布，绘制静态网格
2. 实现蛇的数据结构与渲染
3. 实现主循环 + 键盘输入，让蛇动起来
4. 加入食物生成与吃食物增长
5. 加入碰撞检测（撞墙 / 自撞）与游戏结束
6. 加入计分、最高分、难度递增
7. 完善状态机（开始/暂停/重启）与 UI
8. （可选）音效、移动端适配、视觉打磨

---

## 七、关键设计要点小结

- 采用**网格坐标系**，移动按格子为单位，逻辑简单清晰
- 蛇身用**数组 + 头插尾删**实现，增长即「暂缓删尾」
- **先移尾再判自撞**，避免合法移动被误判
- 状态机集中管理生命周期，输入、循环、碰撞各司其职
- 更新与渲染分离，便于维护和调试

---

## 八、移动端适配

### 8.1 所需模块

| 模块 | 职责 | 关键点 |
|------|------|--------|
| M1 响应式画布 | 固定尺寸 → 自适应视口 | `width:400px; max-width:100%; height:auto` 保持正方形 |
| M2 触控输入 | 滑动转向 | 复用 `queueDirection()`，无需改核心逻辑 |
| M3 触控人体工学 | 防缩放 / 防滚动 / 防误触 | `touch-action`、去高亮、`overscroll-behavior`、≥44px 按钮 |
| M4 布局与文案 | 小屏重排、提示更新 | 按钮换行、竖屏上下分区 |
| M5 输入共存 | 键盘 + 触控并存 | 同一套方向数据结构，互不冲突 |

### 8.2 关键实现

- **滑动检测**：`touchstart` 记录起点，`touchend` 计算位移；位移低于 20px 视为点击忽略；取位移更大的轴向映射为 `{x,y}` 方向向量。
- **复用入队**：触控产生 `{x,y}` 后同样走 `queueDirection()`，反向保护 / 长度限制 / 超时丢弃自动生效。
- **防误触**：画布 `touch-action:none` 阻止滚动缩放；按钮 `touch-action:manipulation` 防双击缩放；`overscroll-behavior:none` 防下拉回弹；`user-select:none` 防滑动选中。
- **三栏窄屏重排**：`@media (max-width:900px)` 下 `.game` 置 `order:-1` 让游戏主体置顶，历史榜/胜利目标面板随之下移并占满可用宽度（`width:100%`），同时收窄 `.layout` 间距。

### 8.3 实施步骤

1. 响应式画布（CSS 缩放）
2. 滑动输入（touchstart/touchend → `queueDirection`）
3. 触控优化（`touch-action`、去高亮、防滚动）
4. 布局与文案适配（小屏重排、提示更新）
5. 真机测试（iOS Safari / Android Chrome）

### 8.4 已知取舍

- 当前采用 **CSS 缩放（方案 A）**：实现简单，高 DPI 屏画布略有模糊；如需锐利可升级为 JS `devicePixelRatio` 方案。
- 虚拟方向键（D-pad）暂未实现，滑动不习惯时可后续补充。

---

## 九、胜利判定与可配置目标

### 9.1 判定规则

- 蛇每吃一个食物长度 +1；当 `snake.length >= winLength` 时进入 `win` 状态，停止循环并显示胜利遮罩。
- 默认目标为地图容纳上限 `COLS × ROWS`（20×20 = 400），即「填满地图」即胜。
- 判定发生在 `step()` 吃食物增长之后、`spawnFood()` 之前（此时已无空位）。

### 9.2 可配置常量

- 变量 `winLength`，范围 `[4, 400]`，默认 `WIN_DEFAULT = 400`。
- `clampWin()` 统一夹取到范围内；`setWinLength()` 更新变量、输入框与持久化。

### 9.3 右侧设置面板

- 布局：`.layout`（flex row + `flex-wrap`）包裹 `.game` 与右侧 `.panel`，窄屏自动换行到下方。
- 控件：`-` / `+` 步进（步长 5）、数字直输框、预设按钮（20 / 50 / 100 / 200 / 400）。
- 持久化：`localStorage('snake-win')`，读写均套 try/catch。
- 主题：面板全部使用 CSS 变量（`--panel` / `--text` / `--border` / `--muted` 等），深/浅色自动适配。

### 9.4 状态机扩展

- 新增 `win` 状态，与 `over` 同构（清计时器 + 绘制 + 刷新遮罩）。
- 空格键与各按钮的重开入口将 `win` 与 `ready` / `over` 同等对待。

---

## 十、AI 演示（搜索 bot）

### 10.1 目标

内置一个可开关的 AI，作为演示自动游玩。验收标准为「连跑多局均 ≥15 分」；实测 50 局随机食物 **100% 达标，最低 31、中位 150、均值 136**。

### 10.2 算法（确定性搜索，非学习）

每步由 `aiNextDir()` 决策下一步方向，按优先级：

1. **寻路到食物**：BFS 求「蛇头 → 食物」最短路径，障碍 = 墙 + 蛇身（尾部视为空位，因尾部会移动）。
2. **安全校验 `safeToFollow`**：模拟沿路径走到底后，用 BFS 判断蛇头是否仍能连通到蛇尾，避免把自己围死。
3. **跟随蛇尾**：无安全路径时，BFS 跟随蛇尾绕圈等待机会。
4. **兜底 `bestOpenSpaceMove`**：以上都失败时，选「剩余可达空间（洪水填充）最大」的一步。

### 10.3 接入方式（核心逻辑零改动）

- UI 新增「AI 演示」开关按钮；开启后 `step()` 内 `aiMode` 分支调用 `aiNextDir()` 直接设置 `direction`，并清空输入队列。
- 键盘/触控在 `aiMode` 下被忽略（`keydown`/`touchend` 加 `!aiMode` 守卫），与「M5 输入共存」原则一致。
- 复用现有 `step()` / `draw()` / `renderState()` 与状态机，不改移动、碰撞、计分逻辑。

### 10.4 关键辅助函数

| 函数 | 职责 |
|------|------|
| `bfs(from, to, obstacles)` | 最短路径（BFS） |
| `floodFill(from, obstacles)` | 统计可达空格数 |
| `safeToFollow(foodPath)` | 模拟吃食后能否连通蛇尾 |
| `dirFromTo(a, b)` | 相邻两点换算方向向量 |
| `bestOpenSpaceMove()` | 存活兜底：选空间最大一步 |
| `aiNextDir()` | 决策主入口 |

### 10.5 验证与取舍

- 确定性、零依赖、训练成本为零；50 局实测全部 ≥15 分。
- 蛇在无安全路径时靠「跟随蛇尾」长期存活，几乎不死，适合持续演示。
- 若后续需要「神经网络训练」形式，可升级为 DQN（见分析中的方案 B），成本为离线训练 + JS 导出。

---

## 十一、历史得分记录

### 11.1 目标

记录每局最终得分，界面常驻显示**前 10 高分**，可弹出**完整列表**；每局结束自动追加。

### 11.2 数据模型与持久化

- localStorage key `snake-history`，值为数组，每条 `{ score, ts, result }`（`result` ∈ `'over' | 'win'`）。
- 存储上限 `HISTORY_MAX = 100` 条，超出截断；读写均套 try/catch（与 `snake-win`/`snake-theme` 一致）。
- `loadHistory()` 对脏数据做防御：仅保留 `score`、`ts` 均为 number 的合法项。

### 11.3 追加规则（`recordScore`）

- 在 `gameOver()` / `win()` 两个终态末尾调用 `recordScore(score, result)`，一局仅进入一次终态，天然不重复。
- 过滤条件：本局曾使用 AI（`gameUsedAi`）**不计入**；`score <= 0` **不记录**（避免 0 分噪声）。
- `gameUsedAi` 为局级标志：`start()` 置 `false`，`step()` 中一旦 `aiMode` 为真即永久置 `true`。因允许中途开关 AI，用「沾过即作废」而非「当前是否 AI」判定，堵住「AI 玩到高分后中途关 AI 洗白成绩」的漏洞。
- 追加后 `sort`（分数降序，同分 `ts` 新在前）→ 截断到上限 → `saveHistory()` → `renderHistory()`。

### 11.4 界面显示

- **前 10 高分**：独立 `.panel`，位于游戏主体**左侧**（三栏布局：历史榜 | 游戏主体 | 胜利目标，随 `.layout` 的 `flex-wrap` 小屏换行）。渲染 `history.slice(0, 10)`，每行 `#排名 得分 时间`，胜利局加「胜」标记；空态显示「暂无记录」并禁用「查看全部」。
- **完整列表**：专用 modal（`.modal`/`.modal-box`），渲染全部记录，滚动区（`max-height` + `overflow-y:auto`）。打开方式：点「查看全部」；关闭：点「×」、点背景、或 Esc（Esc 优先关 modal，否则切暂停）。
- 时间格式 `MM-DD HH:mm`（`formatTime`）。
- **成绩有效性提示**：游戏主体分数下方常驻「本局成绩有效 / 无效」提示，由 `renderValidity()` 依据 `gameUsedAi` 渲染（有效绿 `--snake`、无效红 `--food`）；在 `start()`（重置有效）与 `step()` 沾 AI（置无效）时刷新。

### 11.5 关键实现

| 函数 | 职责 |
|------|------|
| `loadHistory` / `saveHistory` | localStorage 读写（含容错） |
| `recordScore(score, result)` | 过滤 + 追加 + 排序 + 截断 + 持久化 + 刷新 |
| `renderHistory()` | 统一刷新前 10 与完整列表、空态、按钮状态 |
| `historyItemEl(rank, r)` | 构造单条列表 DOM |
| `formatTime(ts)` | 时间戳格式化 |
| `renderValidity()` | 依据 `gameUsedAi` 渲染成绩有效性提示 |

- 渲染采用 DOM 构建（`createElement`）而非字符串拼接，避免 XSS（分数/时间均为自产数据，风险低但保持习惯）。
